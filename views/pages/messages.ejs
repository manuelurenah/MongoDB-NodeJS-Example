<div class="jumbotron text-center">
    <h1>All Messages</h1>
</div>

<div class="row">
    <div class="col-xs-4">
        <a href="/messages/add" class="btn btn-lg btn-primary">Add New Message</a>
    </div>
</div>
<br />

<table class="table table-bordered table-hover table-striped">
    <thead>
        <tr>
            <th>Message</th>
            <th>Date Published</th>
            <th>Created By</th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        <% for (var message of messages) { %>
            <tr>
                <td><%= message.message %></td>
                <td><%= message.datePublished %></td>
                <td><%= message.createdBy %></td>
                <td>
                    <a href="/messages/<%= message.id %>" class="btn btn-sm btn-info">View</a>
                    <a href="/messages/<%= message.id %>/delete" class="btn btn-sm btn-danger">Delete</a>
                </td>
            </tr>
        <% } %>
    </tbody>
</table>
